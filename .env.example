# This is an example .env file.
# For your local development, copy this file to a new file named .env
# and fill in the actual values. The .env file is ignored by git.

# --- Django Core Settings ---
# IMPORTANT: Generate a new, strong secret key for production.
# You can use an online generator or Django's `get_random_secret_key()` utility.
SECRET_KEY=your-insecure-development-secret-key

# Set to False in production
DEBUG=True

# Comma-separated list of hosts. For production, set this to your domain name.
ALLOWED_HOSTS=127.0.0.1,localhost

# --- Database Settings ---
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=jelani_db
DB_USER=jelani_user
DB_PASSWORD=your_database_password

# --- Email Settings ---
# For development (using console backend), these are not strictly needed.
# For production, configure your SMTP provider's details here.
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
EMAIL_HOST=smtp.yourprovider.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-smtp-username
EMAIL_HOST_PASSWORD=your-smtp-password